apiVersion: v1
kind: Service
metadata:
  name: {{ include "babyfolio-storage-optimization.fullname" . }}-backend
  labels:
    {{- include "babyfolio-storage-optimization.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "babyfolio-storage-optimization.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
